# Port Manager 发布说明

## 📦 版本信息
- **版本号**: v1.0.0
- **发布日期**: 2026-02-08
- **支持平台**: macOS 10.15+, Windows 10+
- **Java要求**: Java 11+

---

## 🎁 发布包内容

本发布包是**开箱即用**的可执行版本，无需任何配置。

```
release/
├── PortManager.command       # Mac启动脚本（双击运行）
├── PortManager.bat          # Windows启动脚本（双击运行）
├── port-manager-web.jar     # Spring Boot应用程序
├── README.txt               # 详细使用文档
└── 快速启动指南.md           # 快速上手指南
```

---

## 🚀 分发方式

### 方式1: 直接压缩分发
```bash
# 创建ZIP压缩包
cd /Users/panris/project/port-manager/port-manager-web
zip -r PortManager-v1.0.0.zip release/

# 分发给用户
# 用户解压后双击对应系统的启动脚本即可
```

### 方式2: 分别为不同系统创建独立包

#### Mac版本
```bash
cd release
zip PortManager-macOS-v1.0.0.zip \
    PortManager.command \
    port-manager-web.jar \
    README.txt \
    快速启动指南.md
```

#### Windows版本
```bash
cd release
zip PortManager-Windows-v1.0.0.zip \
    PortManager.bat \
    port-manager-web.jar \
    README.txt \
    快速启动指南.md
```

---

## 📝 用户使用步骤

### Mac用户
1. 解压 `PortManager-v1.0.0.zip`
2. 进入 `release` 文件夹
3. 双击 `PortManager.command`
4. 首次运行可能需要右键→打开（macOS安全限制）
5. 浏览器自动打开 http://localhost:9527

### Windows用户
1. 解压 `PortManager-v1.0.0.zip`
2. 进入 `release` 文件夹
3. 双击 `PortManager.bat`
4. 如有安全提示，点击"仍要运行"
5. 浏览器自动打开 http://localhost:9527

---

## 🎯 脚本特性

### 自动化功能
- ✅ Java环境检测（未安装会提示）
- ✅ 端口占用检查（自动清理）
- ✅ 应用启动监控（失败会提示）
- ✅ 浏览器自动打开
- ✅ 进程管理（Ctrl+C优雅退出）

### 用户体验
- ✅ 中文界面和提示
- ✅ 彩色图标和状态显示
- ✅ 清晰的错误提示和解决方案
- ✅ 一键启动，无需配置

### 安全性
- ✅ 端口占用提示（需确认才关闭其他进程）
- ✅ 优雅的进程停止机制
- ✅ 不修改系统设置
- ✅ 完全独立运行

---

## 🛠️ 系统要求

### 必需条件
- **Java**: 11或更高版本
- **内存**: 建议256MB+
- **端口**: 9527端口可用

### 可选条件
- **浏览器**: 任意现代浏览器（Chrome、Safari、Firefox、Edge等）

---

## 📚 附加文档

发布包中包含完整的功能文档：

1. **README.txt** - 完整使用说明
2. **快速启动指南.md** - 快速上手指南
3. **SYSTEM_SERVICES.md** - 系统服务管理指南（在项目根目录）
4. **PERMANENT_STOP_GUIDE.md** - 永久停止功能说明（在项目根目录）
5. **BATCH_KILL_GUIDE.md** - 批量关闭功能指南（在项目根目录）

---

## 🎨 应用功能

### 核心功能
- 📊 实时监控所有端口占用
- 🔍 多维度筛选（端口类型、进程类型、协议等）
- ❌ 单个/批量关闭进程
- 🛑 永久停止系统服务
- 🔄 自动刷新端口列表
- 🌓 浅色/深色主题切换

### 智能识别
- 自动识别开发进程（Node.js、Python、Java等）
- 自动识别数据库服务（MySQL、Redis、PostgreSQL等）
- 自动识别Web服务器（Nginx、Apache等）
- 自动识别系统进程

---

## ⚙️ 技术架构

- **后端**: Spring Boot 2.7.18 + Java 11
- **前端**: 原生JavaScript + HTML5 + CSS3
- **端口扫描**: lsof (macOS) / netstat (Windows)
- **进程管理**: kill/brew services (macOS) / taskkill (Windows)

---

## 🔧 高级使用

### 命令行启动（跳过脚本）

#### Mac/Linux
```bash
cd release
java -jar port-manager-web.jar
```

#### Windows
```cmd
cd release
java -jar port-manager-web.jar
```

### 自定义端口
```bash
java -jar port-manager-web.jar --server.port=8080
```

### 查看日志
```bash
java -jar port-manager-web.jar > app.log 2>&1
```

---

## 📊 发布检查清单

- [x] Mac启动脚本 (PortManager.command)
- [x] Windows启动脚本 (PortManager.bat)
- [x] 应用程序JAR (port-manager-web.jar)
- [x] 使用说明文档 (README.txt)
- [x] 快速启动指南 (快速启动指南.md)
- [x] Mac脚本执行权限 (chmod +x)
- [x] 中文界面和提示
- [x] 自动打开浏览器
- [x] 错误处理和提示
- [x] 优雅退出机制

---

## 🎉 分发建议

### 分发渠道
1. **GitHub Releases** - 上传ZIP压缩包
2. **官网下载** - 提供直接下载链接
3. **内部分发** - 通过企业内网分发

### 文件命名
```
PortManager-v1.0.0-All.zip          # 全平台版本
PortManager-v1.0.0-macOS.zip        # Mac专用版本
PortManager-v1.0.0-Windows.zip      # Windows专用版本
```

### 下载页面示例文案
```markdown
# 下载 Port Manager

## 最新版本: v1.0.0

### 全平台版本（推荐）
- [PortManager-v1.0.0-All.zip](链接) - 19.5MB
- 支持 macOS 和 Windows
- 包含所有启动脚本

### 系统专用版本
- [PortManager-v1.0.0-macOS.zip](链接) - 19.5MB - 仅Mac用户
- [PortManager-v1.0.0-Windows.zip](链接) - 19.5MB - 仅Windows用户

### 使用方法
1. 下载对应版本
2. 解压到任意目录
3. 双击启动脚本（Mac: PortManager.command / Windows: PortManager.bat）
4. 浏览器自动打开管理页面

### 系统要求
- Java 11 或更高版本
- 如未安装Java：[Mac安装指南](链接) | [Windows安装指南](链接)
```

---

## 📞 支持信息

- **项目地址**: /Users/panris/project/port-manager/port-manager-web
- **应用端口**: 9527
- **访问地址**: http://localhost:9527

---

## 🎊 发布完成

发布包已准备就绪，可以立即分发给用户使用！

**下一步**: 创建ZIP压缩包并分发 🚀